"use client";

import { Building2, Check, Landmark, Plus, Save, Settings2 } from "lucide-react";
import { useState } from "react";

const baseServices = ["Aseo urbano", "Gas", "Publicidad", "Electricidad", "Agua", "Solvencias municipales"];
const municipalities = [{ name: "Municipio principal", branch: "Casa matriz", activity: "Comercio al mayor", factor: "0,80 %", source: "Ordenanza por cargar", valid: "Vigencia por confirmar" }, { name: "Municipio de la sucursal", branch: "Sucursal", activity: "Comercio al detal", factor: "1,20 %", source: "Ordenanza por cargar", valid: "Vigencia por confirmar" }];

export function CompanyTaxSettings() { const [enabled, setEnabled] = useState(() => new Set(baseServices)); const [saved, setSaved] = useState(false); const toggle = (name: string) => setEnabled((current) => { const next = new Set(current); next.has(name) ? next.delete(name) : next.add(name); return next; }); return <div className="mx-auto max-w-6xl px-4 py-7 sm:px-6 lg:px-10"><div className="flex flex-col gap-4 border-b border-stone-200 pb-6 dark:border-stone-800 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm text-stone-500">Empresa activa / Configuración</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Cobertura tributaria y servicios</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-stone-600 dark:text-stone-300">Define qué servicios se controlan para esta empresa y las reglas municipales que alimentan las declaraciones por sucursal.</p></div><button className="inline-flex h-9 items-center gap-2 rounded-lg bg-[#14352d] px-3 text-sm font-medium text-white hover:bg-[#0e2821]" onClick={() => setSaved(true)} type="button"><Save size={16} /> Guardar configuración</button></div><section className="mt-6 rounded-xl border border-stone-200 bg-white shadow-sm dark:border-stone-800 dark:bg-stone-900"><div className="border-b border-stone-100 p-5 dark:border-stone-800"><div className="flex gap-3"><div className="grid size-9 place-items-center rounded-lg bg-[#e7f0e9] text-[#14352d] dark:bg-emerald-950 dark:text-emerald-200"><Settings2 size={18} /></div><div><h2 className="font-semibold">Servicios habilitados</h2><p className="mt-1 text-sm text-stone-500">Solo los servicios habilitados aparecerán al registrar documentos y en el calendario de la empresa.</p></div></div></div><div className="grid gap-3 p-5 sm:grid-cols-2 lg:grid-cols-3">{baseServices.map((name) => <button className={`flex items-center justify-between rounded-lg border p-3 text-left text-sm transition ${enabled.has(name) ? "border-emerald-300 bg-emerald-50/70 dark:border-emerald-900 dark:bg-emerald-950/30" : "border-stone-200 dark:border-stone-700"}`} key={name} onClick={() => { toggle(name); setSaved(false); }} type="button"><span className="font-medium">{name}</span><span className={`grid size-5 place-items-center rounded-full ${enabled.has(name) ? "bg-emerald-600 text-white" : "bg-stone-100 text-stone-400 dark:bg-stone-800"}`}>{enabled.has(name) && <Check size={13} />}</span></button>)}</div></section><section className="mt-6 overflow-hidden rounded-xl border border-stone-200 bg-white shadow-sm dark:border-stone-800 dark:bg-stone-900"><div className="flex flex-col gap-3 border-b border-stone-100 p-5 dark:border-stone-800 sm:flex-row sm:items-center sm:justify-between"><div className="flex gap-3"><div className="grid size-9 place-items-center rounded-lg bg-[#e7f0e9] text-[#14352d] dark:bg-emerald-950 dark:text-emerald-200"><Landmark size={18} /></div><div><h2 className="font-semibold">Alcaldías y factores aplicables</h2><p className="mt-1 text-sm text-stone-500">Cada sucursal requiere su jurisdicción, actividad económica, factor, fuente y vigencia.</p></div></div><button className="inline-flex h-9 items-center gap-2 rounded-lg border border-stone-200 px-3 text-sm font-medium hover:bg-stone-50 dark:border-stone-700 dark:hover:bg-stone-800" type="button"><Plus size={16} /> Agregar alcaldía</button></div><div className="divide-y divide-stone-100 dark:divide-stone-800">{municipalities.map((item) => <div className="grid gap-4 p-5 lg:grid-cols-[minmax(0,1.4fr)_minmax(0,1fr)_9rem_minmax(0,1fr)] lg:items-center" key={item.name}><div><div className="flex items-center gap-2"><Building2 size={16} className="text-[#14352d] dark:text-emerald-200" /><p className="font-semibold">{item.name}</p></div><p className="mt-1 text-sm text-stone-500">{item.branch} · {item.activity}</p></div><div><p className="text-xs text-stone-500">Factor aplicable a ventas</p><p className="mt-1 font-semibold">{item.factor}</p></div><div><p className="text-xs text-stone-500">Fuente</p><p className="mt-1 text-sm text-amber-700 dark:text-amber-300">{item.source}</p></div><div><p className="text-xs text-stone-500">Vigencia</p><p className="mt-1 text-sm">{item.valid}</p></div></div>)}</div></section>{saved && <p className="mt-4 text-sm font-medium text-emerald-700 dark:text-emerald-300">Configuración guardada. Las nuevas declaraciones usarán la versión vigente de estas reglas.</p>}</div>; }
