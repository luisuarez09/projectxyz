"use client";;
import { AttachmentInput } from "@/components/ui/attachment-input";
import { AlertTriangle, ArrowLeft, Check, CheckCircle2, FileUp, Landmark, Save, ShieldCheck, Upload, Users } from "lucide-react";
import Link from "next/link";
import { useMemo, useState } from "react";

import { Input } from "@/components/ui/input";
import { SimpleSelect } from "@/components/ui/simple-select";
import { Table, TableHeader, TableRow, TableHead, TableBody, TableCell, TableFooter } from "@/components/ui/table";

type Employee = { id: string; name: string; salary: number; startedAt: string };
const employees: Employee[] = [
  { id: "wilmer", name: "Wilmer Alexander Chavez Lopez", salary: 1300, startedAt: "2019-12-10" },
  { id: "hendry", name: "Hendry Jose Ortiz Garcia", salary: 1300, startedAt: "2025-12-10" },
  { id: "carla", name: "Carla Carolina Daboin Martinez", salary: 1300, startedAt: "2026-01-09" },
  { id: "juan", name: "Juan Diego Cubillan Leon", salary: 1300, startedAt: "2026-01-09" },
  { id: "nathaly", name: "Nathaly Mariu Parra Montilla", salary: 1300, startedAt: "2025-12-10" },
  { id: "roque", name: "Roque Fuenmayor Luis Enrique", salary: 1300, startedAt: "2026-02-02" },
];
const money = new Intl.NumberFormat("es-VE", { style: "currency", currency: "VES", minimumFractionDigits: 2 });
const monthLabel = new Intl.DateTimeFormat("es-VE", { month: "long", year: "numeric" });

function mondaysInMonth(period: string, startedAt: string) {
  const [year, month] = period.split("-").map(Number); const first = new Date(year, month - 1, 1); const last = new Date(year, month, 0); const started = new Date(`${startedAt}T00:00:00`); let total = 0;
  for (const date = new Date(first); date <= last; date.setDate(date.getDate() + 1)) if (date.getDay() === 1 && date >= started) total += 1;
  return total;
}

export function IvssDeclarationWorkspace() {
  const [period, setPeriod] = useState("2026-06"); const [included, setIncluded] = useState(() => new Set(employees.map((employee) => employee.id))); const [invoiceName, setInvoiceName] = useState<string | null>(null); const [invoiceAmount, setInvoiceAmount] = useState(""); const [reviewed, setReviewed] = useState(false);
  const rows = useMemo(() => employees.map((employee) => { const mondays = mondaysInMonth(period, employee.startedAt); const weekly = employee.salary * 12 / 52; const ssoRetention = weekly / 2 * .04 * mondays; const rpeRetention = weekly * .005 * mondays; const ssoContribution = weekly / 2 * .1 * mondays; const rpeContribution = weekly * .02 * mondays; return { ...employee, mondays, ssoRetention, rpeRetention, ssoContribution, rpeContribution, total: ssoRetention + rpeRetention + ssoContribution + rpeContribution }; }), [period]);
  const selected = rows.filter((row) => included.has(row.id)); const totals = selected.reduce((total, row) => ({ ssoRetention: total.ssoRetention + row.ssoRetention, rpeRetention: total.rpeRetention + row.rpeRetention, ssoContribution: total.ssoContribution + row.ssoContribution, rpeContribution: total.rpeContribution + row.rpeContribution, total: total.total + row.total }), { ssoRetention: 0, rpeRetention: 0, ssoContribution: 0, rpeContribution: 0, total: 0 });
  const declared = Number(invoiceAmount.replace(",", ".")); const hasInvoice = invoiceAmount.trim() !== "" && Number.isFinite(declared); const difference = hasInvoice ? declared - totals.total : 0; const matched = hasInvoice && Math.abs(difference) < .01; const [year, month] = period.split("-").map(Number);
  const toggle = (id: string) => setIncluded((current) => { const next = new Set(current); next.has(id) ? next.delete(id) : next.add(id); return next; });
  return (
    <div className="mx-auto max-w-7xl px-4 py-7 sm:px-6 lg:px-10">
      <div className="flex flex-col gap-5 border-b border-stone-200 pb-6 dark:border-stone-800 lg:flex-row lg:items-end lg:justify-between"><div><Link className="inline-flex items-center gap-1 text-sm text-stone-500 hover:text-[#14352d] dark:hover:text-emerald-200" href="/declaraciones"><ArrowLeft size={15} /> Declaraciones</Link><div className="mt-4 flex flex-wrap items-center gap-2"><p className="text-sm text-stone-500">Declaraciones / IVSS</p><span className="rounded-full border border-sky-200 bg-sky-50 px-2.5 py-1 text-xs font-semibold text-sky-700 dark:border-sky-900 dark:bg-sky-950 dark:text-sky-300">Borrador en preparación</span></div><h1 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">IVSS · Seguro Social</h1><p className="mt-2 text-sm text-stone-600 dark:text-stone-300">Nueva Confitería del Sur, C.A. <span className="mx-1 text-stone-300">·</span> RIF J-30995144-0</p></div><div className="flex flex-wrap gap-2"><button className="inline-flex h-9 items-center gap-2 rounded-lg border border-stone-200 bg-white px-3 text-sm font-medium shadow-sm hover:bg-stone-50 dark:border-stone-700 dark:bg-stone-900 dark:hover:bg-stone-800" type="button"><Save size={16} /> Guardar borrador</button><button className="inline-flex h-9 items-center gap-2 rounded-lg bg-[#14352d] px-3 text-sm font-medium text-white shadow-sm hover:bg-[#0e2821]" onClick={() => setReviewed(true)} type="button"><Check size={16} /> Marcar listo para revisión</button></div></div>
      <section className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-[minmax(0,1fr)_21rem]"><div className="rounded-xl border border-stone-200 bg-white p-5 shadow-sm dark:border-stone-800 dark:bg-stone-900"><div className="flex gap-3"><div className="grid size-9 shrink-0 place-items-center rounded-lg bg-[#e7f0e9] text-[#14352d] dark:bg-emerald-950 dark:text-emerald-200"><Landmark size={18} /></div><div><h2 className="font-semibold">Base de cálculo del período</h2><p className="mt-1 text-sm leading-5 text-stone-500">Los lunes se determinan automáticamente dentro del mes y desde la fecha de ingreso de cada empleado.</p></div></div><label className="mt-4 block max-w-xs text-sm font-medium">Período<SimpleSelect className="field mt-1.5" onChange={(event) => setPeriod(event.target.value)} value={period}><option value="2026-05">Mayo 2026</option><option value="2026-06">Junio 2026</option><option value="2026-07">Julio 2026</option></SimpleSelect></label></div><div className="rounded-xl border border-amber-200 bg-amber-50/70 p-5 text-sm text-amber-950 dark:border-amber-900 dark:bg-amber-950/25 dark:text-amber-100"><div className="flex gap-3"><AlertTriangle className="mt-0.5 shrink-0" size={18} /><div><p className="font-semibold">Regla por validar</p><p className="mt-1 leading-5">Las tasas visibles reproducen el formato suministrado. Antes de automatizarlas deberán guardarse con fuente y vigencia.</p></div></div></div></section>
      <div className="mt-6 grid gap-5 xl:grid-cols-[minmax(0,1fr)_21rem]"><section className="overflow-hidden rounded-xl border border-stone-200 bg-white shadow-sm dark:border-stone-800 dark:bg-stone-900"><div className="flex flex-col gap-3 border-b border-stone-100 p-5 dark:border-stone-800 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2"><Users size={18} className="text-[#14352d] dark:text-emerald-200" /><h2 className="font-semibold">Personal incluido</h2></div><p className="mt-1 text-sm text-stone-500">Nombre y salario mensual provendrán del módulo de empleados cuando esté conectado.</p></div><p className="text-sm text-stone-500">{selected.length} de {rows.length} empleados</p></div><div className="overflow-x-auto"><Table className="min-w-[800px] w-full text-left text-sm"><TableHeader className="bg-stone-50 text-xs font-medium text-stone-500 dark:bg-stone-800/70"><TableRow><TableHead className="w-11 px-4 py-3" /><TableHead className="px-3 py-3">Empleado</TableHead><TableHead className="px-3 py-3 text-right">Salario mensual</TableHead><TableHead className="px-3 py-3 text-center">Lunes</TableHead><TableHead className="px-3 py-3 text-right">Retenciones</TableHead><TableHead className="px-3 py-3 text-right">Aportes</TableHead><TableHead className="px-5 py-3 text-right">Total</TableHead></TableRow></TableHeader><TableBody className="divide-y divide-stone-100 dark:divide-stone-800">{rows.map((row) => <TableRow className={included.has(row.id) ? "" : "opacity-45"} key={row.id}><TableCell className="px-4 py-4"><input aria-label={`Incluir ${row.name}`} checked={included.has(row.id)} className="size-4 accent-[#14352d]" onChange={() => toggle(row.id)} type="checkbox" /></TableCell><TableCell className="px-3 py-4"><p className="font-medium">{row.name}</p><p className="mt-1 text-xs text-stone-500">Ingreso: {new Intl.DateTimeFormat("es-VE").format(new Date(`${row.startedAt}T00:00:00`))}</p></TableCell><TableCell className="px-3 py-4 text-right font-medium tabular-nums">{money.format(row.salary)}</TableCell><TableCell className="px-3 py-4 text-center font-medium tabular-nums">{row.mondays}</TableCell><TableCell className="px-3 py-4 text-right"><p className="tabular-nums">SSO {money.format(row.ssoRetention)}</p><p className="mt-1 text-xs text-stone-500 tabular-nums">RPE {money.format(row.rpeRetention)}</p></TableCell><TableCell className="px-3 py-4 text-right"><p className="tabular-nums">SSO {money.format(row.ssoContribution)}</p><p className="mt-1 text-xs text-stone-500 tabular-nums">RPE {money.format(row.rpeContribution)}</p></TableCell><TableCell className="px-5 py-4 text-right font-semibold tabular-nums text-[#14352d] dark:text-emerald-200">{money.format(row.total)}</TableCell></TableRow>)}</TableBody><TableFooter className="border-t border-stone-200 bg-stone-50/70 dark:border-stone-800 dark:bg-stone-800/50"><TableRow><TableCell colSpan={4} className="px-5 py-4"><p className="font-semibold">Total calculado</p><p className="mt-1 text-xs text-stone-500">Retenciones y aportes del personal seleccionado.</p></TableCell><TableCell className="px-3 py-4 text-right font-medium tabular-nums">{money.format(totals.ssoRetention + totals.rpeRetention)}</TableCell><TableCell className="px-3 py-4 text-right font-medium tabular-nums">{money.format(totals.ssoContribution + totals.rpeContribution)}</TableCell><TableCell className="px-5 py-4 text-right text-lg font-semibold tabular-nums text-[#14352d] dark:text-emerald-200">{money.format(totals.total)}</TableCell></TableRow></TableFooter></Table></div></section>
        <aside className="space-y-4"><section className="rounded-xl border border-stone-200 bg-white p-5 shadow-sm dark:border-stone-800 dark:bg-stone-900"><div className="flex gap-3"><div className="grid size-9 place-items-center rounded-lg bg-[#e7f0e9] text-[#14352d] dark:bg-emerald-950 dark:text-emerald-200"><FileUp size={18} /></div><div><h2 className="font-semibold">Factura del IVSS</h2><p className="mt-1 text-sm text-stone-500">Adjunta la factura descargada del portal.</p></div></div><label className="mt-5 flex cursor-pointer items-center justify-center gap-2 rounded-lg border border-dashed border-stone-300 px-3 py-3 text-sm font-medium text-stone-700 hover:bg-stone-50 dark:border-stone-700 dark:text-stone-200 dark:hover:bg-stone-800"><Upload size={16} /> {invoiceName ? "Reemplazar factura" : "Adjuntar factura"}<AttachmentInput
          accept=".pdf,image/*"
          className="sr-only"
          onChange={(event) => setInvoiceName(event.target.files?.[0]?.name ?? null)} /></label>{invoiceName && <p className="mt-3 truncate text-xs text-emerald-700 dark:text-emerald-300">{invoiceName}</p>}<label className="mt-4 block text-sm font-medium">Monto total de la factura<Input className="field mt-1.5 text-right tabular-nums" inputMode="decimal" onChange={(event) => setInvoiceAmount(event.target.value)} placeholder="0,00" value={invoiceAmount} /></label></section><section className={`rounded-xl border p-5 ${hasInvoice && !matched ? "border-rose-200 bg-rose-50/70 text-rose-950 dark:border-rose-900 dark:bg-rose-950/25 dark:text-rose-100" : "border-stone-200 bg-white dark:border-stone-800 dark:bg-stone-900"}`}><div className="flex items-start gap-3">{matched ? <CheckCircle2 className="mt-0.5 text-emerald-600" size={19} /> : <ShieldCheck className="mt-0.5 text-[#14352d] dark:text-emerald-200" size={19} />}<div><h2 className="font-semibold">Conciliación</h2><p className="mt-1 text-sm leading-5 text-stone-500 dark:text-stone-300">La factura debe coincidir con el total de retenciones y aportes.</p></div></div><div className="mt-5 space-y-3 border-t border-current/10 pt-4"><p className="flex justify-between text-sm text-stone-500 dark:text-stone-300"><span>Retenciones</span><span className="tabular-nums">{money.format(totals.ssoRetention + totals.rpeRetention)}</span></p><p className="flex justify-between text-sm text-stone-500 dark:text-stone-300"><span>Aportes</span><span className="tabular-nums">{money.format(totals.ssoContribution + totals.rpeContribution)}</span></p><p className="flex justify-between font-semibold"><span>Total calculado</span><span className="tabular-nums">{money.format(totals.total)}</span></p>{hasInvoice && <p className="flex justify-between font-semibold"><span>Diferencia</span><span className="tabular-nums">{money.format(difference)}</span></p>}</div>{hasInvoice ? <p className={`mt-5 rounded-lg px-3 py-2 text-xs font-medium ${matched ? "bg-emerald-100 text-emerald-800 dark:bg-emerald-950 dark:text-emerald-200" : "bg-rose-100 text-rose-800 dark:bg-rose-950 dark:text-rose-200"}`}>{matched ? "La factura coincide con el cálculo del período." : "Revisa empleados, lunes o el monto de la factura antes de cerrar."}</p> : <p className="mt-5 text-xs text-stone-500">Carga la factura e ingresa su monto para validar la coincidencia.</p>}</section><section className="rounded-xl border border-stone-200 bg-white p-5 text-sm shadow-sm dark:border-stone-800 dark:bg-stone-900"><p className="font-semibold">Control del expediente</p><div className="mt-4 space-y-3"><p className="flex items-center gap-2"><CheckCircle2 size={16} className="text-emerald-600" /> Lunes calculados para {monthLabel.format(new Date(year, month - 1, 1))}</p><p className="flex items-center gap-2"><CheckCircle2 size={16} className="text-emerald-600" /> Personal seleccionado</p><p className="flex items-center gap-2">{reviewed ? <CheckCircle2 size={16} className="text-emerald-600" /> : <span className="size-4 rounded-full border border-stone-300" />} Revisión interna</p></div></section></aside>
      </div>
    </div>
  );
}
