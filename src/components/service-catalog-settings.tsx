"use client";;
import { CalendarDays, Check, Pencil, Plus, Trash2 } from "lucide-react";
import { useState } from "react";

import { Input } from "@/components/ui/input";
import { Table, TableHeader, TableRow, TableHead, TableBody, TableCell } from "@/components/ui/table";

type Rule = { id: number; type: string; organism: string; deadline: string; active: boolean };
const seed: Rule[] = [
  { id: 1, type: "Electricidad", organism: "Prestador eléctrico", deadline: "Día 10 de cada mes", active: true },
  { id: 2, type: "Agua", organism: "Prestador de agua", deadline: "Día 12 de cada mes", active: true },
  { id: 3, type: "Publicidad", organism: "Alcaldía aplicable", deadline: "Día 8 de cada mes", active: true },
  { id: 4, type: "Gas", organism: "Prestador de gas", deadline: "Día 5 de cada mes", active: true },
  { id: 5, type: "Aseo urbano", organism: "Alcaldía aplicable", deadline: "Día 2 de cada mes", active: true },
];

export function ServiceCatalogSettings() {
  const [rules, setRules] = useState(seed); const [formOpen, setFormOpen] = useState(false); const [removing, setRemoving] = useState<Rule | null>(null); const [editing, setEditing] = useState<Rule | null>(null); const [type, setType] = useState(""); const [organism, setOrganism] = useState(""); const [deadline, setDeadline] = useState("");
  const openCreate = () => { setEditing(null); setType(""); setOrganism(""); setDeadline(""); setFormOpen(true); };
  const openEdit = (rule: Rule) => { setEditing(rule); setType(rule.type); setOrganism(rule.organism); setDeadline(rule.deadline); setFormOpen(true); };
  const save = () => { if (!type || !organism || !deadline) return; if (editing) setRules((items) => items.map((item) => item.id === editing.id ? { ...item, type, organism, deadline } : item)); else setRules((items) => [...items, { id: Date.now(), type, organism, deadline, active: true }]); setFormOpen(false); };
  return <div className="mx-auto max-w-6xl px-4 py-7 sm:px-6 lg:px-10"><div className="flex flex-col gap-4 border-b border-stone-200 pb-6 dark:border-stone-800 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm text-stone-500">Configuración de la firma</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Catálogo de servicios</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-stone-600 dark:text-stone-300">La firma define el tipo de servicio, organismo y vencimiento. Las empresas solo habilitan los servicios que les aplican.</p></div><button className="inline-flex h-9 items-center gap-2 rounded-lg bg-[#14352d] px-3 text-sm font-medium text-white hover:bg-[#0e2821]" onClick={openCreate} type="button"><Plus size={16} /> Crear servicio</button></div><section className="mt-6 overflow-hidden rounded-xl border border-stone-200 bg-white shadow-sm dark:border-stone-800 dark:bg-stone-900"><div className="border-b border-stone-100 p-5 dark:border-stone-800"><h2 className="font-semibold">Reglas disponibles</h2><p className="mt-1 text-sm text-stone-500">Se actualizan en esta vista al crear o editar; su conexión con el calendario dependerá del backend.</p></div><div className="overflow-x-auto"><Table className="min-w-full text-left text-sm"><TableHeader className="bg-stone-50 text-xs font-medium text-stone-500 dark:bg-stone-800/70"><TableRow><TableHead className="px-5 py-3">Tipo de servicio</TableHead><TableHead className="px-3 py-3">Organismo</TableHead><TableHead className="px-3 py-3">Regla de vencimiento</TableHead><TableHead className="px-3 py-3">Estado</TableHead><TableHead className="px-5 py-3" /></TableRow></TableHeader><TableBody className="divide-y divide-stone-100 dark:divide-stone-800">{rules.map((rule) => <TableRow key={rule.id}><TableCell className="px-5 py-4 font-medium">{rule.type}</TableCell><TableCell className="px-3 py-4">{rule.organism}</TableCell><TableCell className="px-3 py-4"><span className="inline-flex items-center gap-1"><CalendarDays size={15} className="text-stone-400" />{rule.deadline}</span></TableCell><TableCell className="px-3 py-4"><button className={`rounded-full px-2.5 py-1 text-xs font-medium ${rule.active ? "bg-emerald-50 text-emerald-700" : "bg-stone-100 text-stone-500"}`} onClick={() => setRules((items) => items.map((item) => item.id === rule.id ? { ...item, active: !item.active } : item))} type="button">{rule.active ? "Habilitado" : "Deshabilitado"}</button></TableCell><TableCell className="px-5 py-4"><div className="flex justify-end gap-1"><button aria-label={`Editar ${rule.type}`} className="grid size-8 place-items-center rounded-lg text-stone-500 hover:bg-stone-100 hover:text-[#14352d] dark:hover:bg-stone-800" onClick={() => openEdit(rule)} type="button"><Pencil size={15} /></button><button aria-label={`Eliminar ${rule.type}`} className="grid size-8 place-items-center rounded-lg text-stone-500 hover:bg-rose-50 hover:text-rose-700 dark:hover:bg-rose-950" onClick={() => setRemoving(rule)} type="button"><Trash2 size={15} /></button></div></TableCell></TableRow>)}</TableBody></Table></div></section>{formOpen && <RuleForm editing={editing} type={type} organism={organism} deadline={deadline} onClose={() => setFormOpen(false)} onSave={save} setType={setType} setOrganism={setOrganism} setDeadline={setDeadline} />}{removing && <div className="fixed inset-0 z-50 grid place-items-center bg-stone-950/35 p-4"><section className="w-full max-w-sm rounded-2xl bg-white p-6 shadow-2xl dark:bg-stone-900"><h2 className="text-lg font-semibold">¿Eliminar servicio?</h2><p className="mt-2 text-sm leading-6 text-stone-500">Eliminarás <span className="font-medium text-stone-800 dark:text-stone-200">{removing.type}</span> del catálogo. Las empresas no podrán habilitarlo en nuevas configuraciones.</p><div className="mt-6 flex justify-end gap-2"><button className="h-9 rounded-lg px-3 text-sm font-medium text-stone-600 hover:bg-stone-100 dark:text-stone-300 dark:hover:bg-stone-800" onClick={() => setRemoving(null)} type="button">Cancelar</button><button className="h-9 rounded-lg bg-rose-600 px-3 text-sm font-medium text-white hover:bg-rose-700" onClick={() => { setRules((items) => items.filter((item) => item.id !== removing.id)); setRemoving(null); }} type="button">Eliminar servicio</button></div></section></div>}</div>;
}
function RuleForm({ editing, type, organism, deadline, setType, setOrganism, setDeadline, onClose, onSave }: { editing: Rule | null; type: string; organism: string; deadline: string; setType: (value: string) => void; setOrganism: (value: string) => void; setDeadline: (value: string) => void; onClose: () => void; onSave: () => void }) { return <div className="fixed inset-0 z-50 grid place-items-center bg-stone-950/35 p-4"><section className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl dark:bg-stone-900"><h2 className="text-lg font-semibold">{editing ? "Editar servicio" : "Crear servicio"}</h2><p className="mt-1 text-sm text-stone-500">Se refleja en esta vista al confirmar.</p><label className="mt-5 block text-sm font-medium">Tipo de servicio<Input className="field mt-1.5" onChange={(event) => setType(event.target.value)} placeholder="Ej. Electricidad" value={type} /></label><label className="mt-4 block text-sm font-medium">Organismo<Input className="field mt-1.5" onChange={(event) => setOrganism(event.target.value)} placeholder="Ej. Prestador eléctrico" value={organism} /></label><label className="mt-4 block text-sm font-medium">Regla de vencimiento<Input className="field mt-1.5" onChange={(event) => setDeadline(event.target.value)} placeholder="Ej. Día 10 de cada mes" value={deadline} /></label><div className="mt-6 flex justify-end gap-2"><button className="h-9 rounded-lg px-3 text-sm font-medium text-stone-600 hover:bg-stone-100" onClick={onClose} type="button">Cancelar</button><button className="inline-flex h-9 items-center gap-2 rounded-lg bg-[#14352d] px-3 text-sm font-medium text-white disabled:opacity-50" disabled={!type || !organism || !deadline} onClick={onSave} type="button"><Check size={16} /> {editing ? "Guardar cambios" : "Crear servicio"}</button></div></section></div>; }
