"use client";

import { Check, FileCheck2, Plus, Percent } from "lucide-react";
import { useState } from "react";

import { DatePicker } from "@/components/ui/date-picker";
import { Input } from "@/components/ui/input";

type VatRate = { id: number; name: string; rate: string; appliesFrom: string; source: string };

const initialRates: VatRate[] = [
  { id: 1, name: "Alícuota general", rate: "16", appliesFrom: "", source: "" },
];

export function VatRateSettings() {
  const [rates, setRates] = useState(initialRates);
  const [saved, setSaved] = useState(false);
  const update = (id: number, field: keyof VatRate, value: string) => { setRates((current) => current.map((rate) => rate.id === id ? { ...rate, [field]: value } : rate)); setSaved(false); };
  return <div><section className="rounded-xl border border-sky-200 bg-sky-50/70 p-4 text-sm text-sky-950 dark:border-sky-900 dark:bg-sky-950/25 dark:text-sky-100"><div className="flex gap-3"><Percent className="mt-0.5 shrink-0" size={18} /><div><p className="font-semibold">Configuración global para todas las empresas</p><p className="mt-1 leading-5">Las alícuotas de IVA se definen en la firma con fuente y vigencia. Las empresas las heredan y no pueden modificarlas individualmente.</p></div></div></section><section className="mt-5 overflow-hidden rounded-xl border border-stone-200 bg-white shadow-sm dark:border-stone-800 dark:bg-stone-900"><div className="flex flex-col gap-3 border-b border-stone-100 p-5 dark:border-stone-800 sm:flex-row sm:items-start sm:justify-between"><div><h2 className="font-semibold">Alícuotas de IVA</h2><p className="mt-1 text-sm text-stone-500">Una modificación debe crear una nueva versión vigente sin alterar períodos anteriores cuando se conecte la persistencia.</p></div><button className="inline-flex h-9 items-center justify-center gap-2 rounded-lg border border-stone-200 px-3 text-sm font-medium hover:bg-stone-50 dark:border-stone-700 dark:hover:bg-stone-800" onClick={() => setRates((current) => [...current, { id: Date.now(), name: "Nueva alícuota", rate: "", appliesFrom: "", source: "" }])} type="button"><Plus size={15} /> Agregar alícuota</button></div><div className="divide-y divide-stone-100 dark:divide-stone-800">{rates.map((rate) => { const ready = Boolean(rate.rate && rate.appliesFrom && rate.source); return <article className="p-5" key={rate.id}><div className="grid gap-4 lg:grid-cols-[minmax(0,1fr)_8rem_11rem_minmax(0,1.4fr)]"><label className="field-label">Nombre<Input className="field mt-1.5" onChange={(event) => update(rate.id, "name", event.target.value)} value={rate.name} /></label><label className="field-label">Porcentaje<Input className="field mt-1.5" inputMode="decimal" onChange={(event) => update(rate.id, "rate", event.target.value)} value={rate.rate} /></label><label className="field-label">Aplicar desde<DatePicker className="field mt-1.5" onChange={(event) => update(rate.id, "appliesFrom", event.target.value)} value={rate.appliesFrom} /></label><label className="field-label">Fuente normativa<Input className="field mt-1.5" onChange={(event) => update(rate.id, "source", event.target.value)} placeholder="Ley, gaceta o providencia validada" value={rate.source} /></label></div><div className={`mt-3 inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-medium ${ready ? "bg-emerald-50 text-emerald-700 dark:bg-emerald-950 dark:text-emerald-300" : "bg-stone-100 text-stone-500 dark:bg-stone-800"}`}><FileCheck2 size={13} /> {ready ? "Lista para activar" : "Borrador · falta trazabilidad"}</div></article>; })}</div><div className="flex items-center justify-between gap-3 border-t border-stone-100 bg-stone-50/60 px-5 py-4 dark:border-stone-800 dark:bg-stone-800/40"><p className="text-xs text-stone-500">El 16% mostrado es un dato demostrativo y no se considera validado mientras falten fuente y vigencia.</p><button className="inline-flex h-9 shrink-0 items-center gap-2 rounded-lg bg-[#14352d] px-3 text-sm font-medium text-white" onClick={() => setSaved(true)} type="button"><Check size={15} /> Guardar alícuotas</button></div></section>{saved && <p className="mt-4 text-sm font-medium text-emerald-700 dark:text-emerald-300">Alícuotas guardadas en esta vista demostrativa.</p>}</div>;
}
